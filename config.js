var conf = {
  // you probably shouldn't change the stuff below
  host : 'services.my511.org',
  port : 80,
  path : '/Transit2.0/GetNextDeparturesByStopCode.aspx?token={token}&stopcode={stopId}'
}

module.exports = conf;
